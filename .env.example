# Root / shared local examples — copy into app paths as needed.
# Never commit real secrets.

# apps/web/.env.local
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8787

# apps/api/.dev.vars
# APP_ORIGIN=http://localhost:3000
# SESSION_SECRET=generate-a-long-random-string
