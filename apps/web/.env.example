# Public env for local web → API
# MUST use same hostname as the browser page (usually localhost, not 127.0.0.1).
# localhost:3000 + 127.0.0.1:8787 is cross-site → session cookie not sent (SameSite=Lax).
NEXT_PUBLIC_API_URL=http://localhost:8787
